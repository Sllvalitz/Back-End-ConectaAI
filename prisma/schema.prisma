datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Artigo {
  id        Int     @id @default(autoincrement())
  titulo    String
  slug      String  @unique
  conteudo  String
  imagemUrl String?
}
